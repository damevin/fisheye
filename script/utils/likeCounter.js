/* function increaseLike(like) {
	const getLikeCount = document.querySelector(
		".photographer-page__gallery__media__footer__like-section-counter"
	);
	let liked = false;
	console.log(like);
	liked = !liked;
	liked ? like-- : like++;
	getLikeCount.innerHTML = `${like}`;
} */
